Add this to dependencies :
    "react-icons": "^4.3.1",

Add this to index.css  :
    body{
        padding-top: 80px;
    }

Put the logo in the images folder & change the path in header.js

Put CustomHooks folder outside the Pages folder and add path in header.js